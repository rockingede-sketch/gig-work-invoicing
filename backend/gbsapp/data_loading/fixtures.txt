# Export your data as fixtures
uv run python manage.py dumpdata > ./databases/initial_data.json

# Then anyone cloning can run:
uv run python manage.py migrate
uv run python manage.py loaddata ./databases/initial_data.json